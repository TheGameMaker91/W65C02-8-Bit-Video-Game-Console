MEMORY {
    RAM:    start = $0200, size = $0600, type = rw;
    ROM:    start = $8000, size = $0800, type = ro, fill = yes, fillval = $FF;
    VEC:    start = $FFFA, size = $0006, type = ro;
}

SEGMENTS {
    CODE:       load = ROM, type = ro;
    VECTORS:    load = VEC, type = ro;
}
