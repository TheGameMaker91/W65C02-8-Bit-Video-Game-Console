MEMORY {
    RAM:    start = $0200, size = $0600, type = rw;
    ROM:    start = $8000, size = $7FFF, type = ro;
}

SEGMENTS {
    CODE:    load = ROM,  type = ro;
    VECTORS: load = ROM, type = ro;
}
